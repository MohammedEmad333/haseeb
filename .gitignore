node_modules/
dist/
dist-ssr/
coverage/
*.local
.DS_Store
.vite/
src-tauri/target/

# The Android project is committed: it is what CI builds, and it carries the
# launcher icons, the manifest and the signing wiring. Capacitor's own
# android/.gitignore excludes the generated parts — build outputs, the copied
# web assets and local.properties.
ios/
*.log
.env
*.jks
*.keystore

# TypeScript incremental build state
*.tsbuildinfo
